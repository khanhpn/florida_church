<%= render "admin/shared/bread_crumb", name: {title: "Usher hospitals", bread_crumb: "Top"} %>
<!-- Main content -->
<section class="content">
  <div class="container-fluid">
    <div class="row">
      <div class="col-12">
        <div class="card">
          <div class="card-header">
            <h3 class="card-title">Manage usher hospitals</h3>
          </div>
          <!-- /.card-header -->
          <div class="card-body">
            <table id="example2" class="table table-bordered table-hover">
              <thead>
                <tr>
                  <th>Name</th>
                  <th>Content</th>
                  <th>Display</th>
                  <th>Display Order</th>
                  <th>Created at</th>
                  <th>Updated at</th>
                  <th>Action</th>
                </tr>
              </thead>
              <tbody>
                <% @usher_hospitals.each do |usher_hospital| %>
                  <tr>
                    <td><%= usher_hospital&.name || "-" %></td>
                    <td><%= usher_hospital&.content&.html_safe || "-"  %></td>
                    <td><%= usher_hospital&.display || "-"  %></td>
                    <td><%= usher_hospital&.display_order || "-"  %></td>
                    <td><%= usher_hospital&.created_at || "-"  %></td>
                    <td><%= usher_hospital&.updated_at || "-"  %></td>
                    <td>
                      <% unless usher_hospital.blank? %>
                        <%= link_to "Edit", edit_admin_ushers_hospital_path(usher_hospital), class: "btn btn-success" %>
                        <%= link_to "Delete", admin_ushers_hospital_path(usher_hospital), method: :delete, class: "btn btn-danger" %>
                      <% end %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
          <!-- /.card-body -->
        </div>
      </div>
      <!-- /.col -->
    </div>
    <!-- /.row -->
  </div>
  <!-- /.container-fluid -->
</section>

<section class="section-btn-group">
  <div class="section-btn-group-wrap">
    <%= link_to "Create", new_admin_ushers_hospital_path, class: "btn btn-info" %>
  </div>
</section>
