<%= render "admin/shared/bread_crumb", name: {title: "Create children", bread_crumb: "New"} %>
<%= form_with model: @children, url: admin_children_path do |form| %>
  <section class="content">
    <div class="container-fluid">
      <%= render "form", form: form %>
      <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
    <div class="section-btn-group-wrap">
      <%= link_to "Back", admin_children_path, class: "btn btn-info" %>
      <%= form.submit "Create", class: "btn btn-success", id: "id_submit_children" %>
    </div>
  </section>
<% end %>
