<%= render "admin/shared/bread_crumb", name: {title: "Edit children", bread_crumb: "Edit"} %>
<%= form_with model: @children, url: admin_child_path(@children), method: :patch do |form| %>
  <section class="content">
    <div class="container-fluid">
      <%= render "form", form: form %>
      <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
    <div class="section-btn-group-wrap">
      <%= link_to "Back", admin_child_path, class: "btn btn-info" %>
      <%= form.submit "Update", class: "btn btn-success", id: "id_submit_children" %>
    </div>
  </section>
<% end %>
