<%= render "admin/shared/bread_crumb", name: {title: "Anoiting", bread_crumb: "Top"} %>
<!-- Main content -->
<section class="content">
  <div class="container-fluid">
    <div class="row">
      <div class="col-12">
        <div class="card">
          <div class="card-header">
            <h3 class="card-title">Manage anoiting</h3>
          </div>
          <!-- /.card-header -->
          <div class="card-body">
            <table id="example2" class="table table-bordered table-hover">
              <thead>
                <tr>
                  <th>Name</th>
                  <th>Display</th>
                  <th>Main article</th>
                  <th>Created at</th>
                  <th>Action</th>
                </tr>
              </thead>
              <tbody>
                <% @anointings.each do |anoiting| %>
                  <tr>
                    <td><%= anoiting&.name || "-" %></td>
                    <td><%= anoiting&.display ? "Yes" : "No"  %></td>
                    <td><%= anoiting&.is_main_article ? "Yes" : "No"  %></td>
                    <td><%= anoiting&.created_at || "-"  %></td>
                    <td>
                      <% unless anoiting.blank? %>
                        <%= link_to "Show", admin_anoiting_path(anoiting), class: "btn btn-primary" %>
                        <%= link_to "Edit", edit_admin_anointing_path(anoiting), class: "btn btn-success" %>
                        <%= link_to "Delete", admin_anointing_path(anoiting), method: :delete, class: "btn btn-danger" %>
                      <% end %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
          <!-- /.card-body -->
        </div>
      </div>
      <!-- /.col -->
    </div>
    <!-- /.row -->
  </div>
  <!-- /.container-fluid -->
</section>
<section class="section-btn-group">
  <div class="section-btn-group-wrap">
    <%= link_to "Create", new_admin_anointing_path, class: "btn btn-info" %>
  </div>
</section>
