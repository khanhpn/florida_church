<%= render "admin/shared/bread_crumb", name: {title: "Show environment church", bread_crumb: "Show"} %>
<section class="content">
  <div class="container-fluid">
    <table id="example2" class="table table-bordered table-hover">
      <thead>
        <tr>
          <th>Name</th>
          <% if @is_main_article %>
            <th>Content</th>
          <% end %>
          <% if !@is_main_article && @environment_church.file_image.present? %>
            <th>Link image</th>
          <% end %>
          <% if !@is_main_article && @environment_church.file_pdf.present? %>
            <th>Link pdf</th>
          <% end %>
          <th>Action</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><%= @environment_church&.name || "-" %></td>
          <% if @is_main_article %>
            <td><%= @environment_church&.content&.html_safe || "-"  %></td>
          <% end %>
          <% if !@is_main_article && @environment_church.file_image.present? %>
            <td><%= link_to 'download', rails_blob_path(@environment_church.file_image, disposition: "download"), target: "_blank" %></td>
          <% end %>
          <% if !@is_main_article && @environment_church.file_pdf.present? %>
            <td><%= link_to 'download', rails_blob_path(@environment_church.file_pdf, disposition: "download"), target: "_blank"  %></td>
          <% end %>
          <td>
            <% unless @environment_church.blank? %>
              <%= link_to "Edit", edit_admin_environment_church_path(@environment_church), class: "btn btn-success" %>
              <%= link_to "Delete", admin_environment_church_path(@environment_church), method: :delete, class: "btn btn-danger" %>
              <%= link_to "Back", admin_environment_church_index_path, class: "btn btn-primary" %>
            <% end %>
          </td>
        </tr>
      </tbody>
    </table>
    <!-- /.row -->
  </div>
  <!-- /.container-fluid -->
</section>
