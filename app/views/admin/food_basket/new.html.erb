<%= render "admin/shared/bread_crumb", name: {title: "Create food basket", bread_crumb: "New"} %>
<%= form_with model: @food_basket, url: admin_food_basket_index_path do |form| %>
  <section class="content">
    <div class="container-fluid">
      <%= render "admin/shared_form/form", form: form, item: @food_basket, title_name: "Create food basket" %>
      <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
    <div class="section-btn-group-wrap">
      <%= link_to "Back", admin_food_basket_index_path, class: "btn btn-info" %>
      <%= form.submit "Create", class: "btn btn-success", id: "id_submit" %>
    </div>
  </section>
<% end %>
