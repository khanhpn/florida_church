<%= render "admin/shared/bread_crumb", name: {title: "Create adoration time", bread_crumb: "New"} %>
<%= form_with model: @adoration_time, url: admin_adoration_times_path do |form| %>
  <section class="content">
    <div class="container-fluid">
      <%= render "form", form: form %>
      <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
    <div class="section-btn-group-wrap">
      <%= link_to "Back", admin_adoration_times_path, class: "btn btn-info" %>
      <%= form.submit "Create", class: "btn btn-success", id: "id_submit_adoration_time" %>
    </div>
  </section>
<% end %>
