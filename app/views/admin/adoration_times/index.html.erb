<%= render "admin/shared/bread_crumb", name: {title: "MassTime", bread_crumb: "Top"} %>
<!-- Main content -->
<section class="content">
  <div class="container-fluid">
    <div class="row">
      <div class="col-12">
        <div class="card">
          <div class="card-header">
            <h3 class="card-title">Manage adoration time</h3>
          </div>
          <!-- /.card-header -->
          <div class="card-body">
            <table id="example2" class="table table-bordered table-hover">
              <thead>
                <tr>
                  <th>Name</th>
                  <th>Content</th>
                  <th>Updated at</th>
                  <th>Action</th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td><%= @adoration_time&.name || "-" %></td>
                  <td><%= @adoration_time&.content&.html_safe || "-"  %></td>
                  <td><%= @adoration_time&.updated_at || "-"  %></td>
                  <td>
                    <% unless @adoration_time.blank? %>
                      <%= link_to "Edit", edit_admin_adoration_time_path(@adoration_time), class: "btn btn-success" %>
                      <%= link_to "Delete", admin_adoration_time_path(@adoration_time), method: :delete, class: "btn btn-danger", data: { confirm: "Are you sure?" } %>
                    <% end %>
                  </td>
                </tr>
              </tbody>
            </table>
          </div>
          <!-- /.card-body -->
        </div>
      </div>
      <!-- /.col -->
    </div>
    <!-- /.row -->
  </div>
  <!-- /.container-fluid -->
</section>
<section class="section-btn-group">
  <div class="section-btn-group-wrap">
    <%= link_to "Create", new_admin_adoration_time_path, class: "btn btn-info" %>
  </div>
</section>
