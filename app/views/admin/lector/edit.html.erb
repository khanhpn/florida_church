<%= render "admin/shared/bread_crumb", name: {title: "Edit lector", bread_crumb: "Edit"} %>
<%= form_with model: @lector, url: admin_lector_path(@lector), method: :patch do |form| %>
  <section class="content">
    <div class="container-fluid">
      <%= render "form", form: form %>
      <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
    <div class="section-btn-group-wrap">
      <%= link_to "Back", admin_lector_index_path, class: "btn btn-info" %>
      <%= form.submit "Update", class: "btn btn-success", id: "id_submit_lector" %>
    </div>
  </section>
<% end %>
