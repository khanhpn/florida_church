<%= render "admin/shared/bread_crumb", name: {title: "Marriage", bread_crumb: "Top"} %>
<!-- Main content -->
<section class="content">
  <div class="container-fluid">
    <div class="row">
      <div class="col-12">
        <div class="card">
          <div class="card-header">
            <h3 class="card-title">Manage marriage</h3>
          </div>
          <!-- /.card-header -->
          <div class="card-body">
            <table id="example2" class="table table-bordered table-hover">
              <thead>
                <tr>
                  <th>Name</th>
                  <th>Display</th>
                  <th>Main article</th>
                  <th>Created at</th>
                  <th>Action</th>
                </tr>
              </thead>
              <tbody>
                <% @marriages.each do |marriage| %>
                  <tr>
                    <td><%= marriage&.name || "-" %></td>
                    <td><%= marriage&.content&.html_safe || "-"  %></td>
                    <td><%= marriage&.display || "-"  %></td>
                    <td><%= marriage&.display_order || "-"  %></td>
                    <td><%= marriage&.created_at || "-"  %></td>
                    <td><%= marriage&.updated_at || "-"  %></td>
                    <td>
                      <% unless marriage.blank? %>
                        <%= link_to "Edit", edit_admin_marriage_path(marriage), class: "btn btn-success" %>
                        <%= link_to "Delete", admin_marriage_path(marriage), method: :delete, class: "btn btn-danger" %>
                      <% end %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
          <!-- /.card-body -->
        </div>
      </div>
      <!-- /.col -->
    </div>
    <!-- /.row -->
  </div>
  <!-- /.container-fluid -->
</section>
<section class="section-btn-group">
  <div class="section-btn-group-wrap">
    <%= link_to "Create", new_admin_marriage_path, class: "btn btn-info" %>
  </div>
</section>
