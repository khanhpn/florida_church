<%= render "admin/shared/bread_crumb", name: {title: "Create hospital visit", bread_crumb: "New"} %>
<%= form_with model: @hospital_visit, url: admin_hospital_visit_index_path do |form| %>
  <section class="content">
    <div class="container-fluid">
      <%= render "admin/shared_form/form", form: form, item: @hospital_visit, title_name: "Create hospital visit" %>
      <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
    <div class="section-btn-group-wrap">
      <%= link_to "Back", admin_hospital_visit_index_path, class: "btn btn-info" %>
      <%= form.submit "Create", class: "btn btn-success", id: "id_submit" %>
    </div>
  </section>
<% end %>
