<%= render "admin/shared/bread_crumb", name: {title: "Council", bread_crumb: "Top"} %>
<!-- Main content -->
<section class="content">
  <div class="container-fluid">
    <div class="row">
      <div class="col-12">
        <div class="card">
          <div class="card-header">
            <h3 class="card-title">Manage council</h3>
          </div>
          <!-- /.card-header -->
          <div class="card-body">
            <table id="example2" class="table table-bordered table-hover">
              <thead>
                <tr>
                  <th>Name</th>
                  <th>Content</th>
                  <th>Display</th>
                  <th>Display Order</th>
                  <th>Created at</th>
                  <th>Updated at</th>
                  <th>Action</th>
                </tr>
              </thead>
              <tbody>
                <% @councils.each do |council| %>
                  <tr>
                    <td><%= link_to council&.name, council&.name_link %></td>
                    <td><%= council&.content&.html_safe || "-"  %></td>
                    <td><%= council&.display || "-"  %></td>
                    <td><%= council&.display_order || "-"  %></td>
                    <td><%= council&.created_at || "-"  %></td>
                    <td><%= council&.updated_at || "-"  %></td>
                    <td>
                      <% unless council.blank? %>
                        <%= link_to "Edit", edit_admin_council_path(council), class: "btn btn-success" %>
                        <%= link_to "Delete", admin_council_path(council), method: :delete, class: "btn btn-danger" %>
                      <% end %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
          <!-- /.card-body -->
        </div>
      </div>
      <!-- /.col -->
    </div>
    <!-- /.row -->
  </div>
  <!-- /.container-fluid -->
</section>
<section class="section-btn-group">
  <div class="section-btn-group-wrap">
    <%= link_to "Create", new_admin_council_path, class: "btn btn-info" %>
  </div>
</section>
