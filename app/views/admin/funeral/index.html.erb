<%= render "admin/shared/bread_crumb", name: {title: "funeral", bread_crumb: "Top"} %>
<!-- Main content -->
<section class="content">
  <div class="container-fluid">
    <div class="row">
      <div class="col-12">
        <div class="card">
          <div class="card-header">
            <h3 class="card-title">Manage funeral</h3>
          </div>
          <!-- /.card-header -->
          <div class="card-body">
            <table id="example2" class="table table-bordered table-hover">
              <thead>
                <tr>
                  <th>Name</th>
                  <th>Display</th>
                  <th>Main article</th>
                  <th>Created at</th>
                  <th>Action</th>
                </tr>
              </thead>
              <tbody>
                <% @funerals.each do |funeral| %>
                  <tr>
                    <td><%= link_to funeral&.name, funeral&.name_link %></td>
                    <td><%= funeral&.display ? "Yes" : "No"  %></td>
                    <td><%= funeral&.is_main_article ? "Yes" : "No"  %></td>
                    <td><%= funeral&.created_at || "-"  %></td>
                    <td>
                      <% unless funeral.blank? %>
                        <%= link_to "Show", admin_funeral_path(funeral), class: "btn btn-primary" %>
                        <%= link_to "Edit", edit_admin_funeral_path(funeral), class: "btn btn-success" %>
                        <%= link_to "Delete", admin_funeral_path(funeral), method: :delete, class: "btn btn-danger" %>
                      <% end %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
          <!-- /.card-body -->
        </div>
      </div>
      <!-- /.col -->
    </div>
    <!-- /.row -->
  </div>
  <!-- /.container-fluid -->
</section>
<section class="section-btn-group">
  <div class="section-btn-group-wrap">
    <%= link_to "Create", new_admin_funeral_path, class: "btn btn-info" %>
  </div>
</section>
