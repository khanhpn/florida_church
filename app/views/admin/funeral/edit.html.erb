<%= render "admin/shared/bread_crumb", name: {title: "Edit funeral", bread_crumb: "Edit"} %>
<%= form_with model: @funeral, url: admin_funeral_path(@funeral), method: :patch do |form| %>
  <section class="content">
    <div class="container-fluid">
      <%= render "form", form: form %>
      <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
    <div class="section-btn-group-wrap">
      <%= link_to "Back", admin_funeral_index_path, class: "btn btn-info" %>
      <%= form.submit "Update", class: "btn btn-success", id: "id_submit_funeral" %>
    </div>
  </section>
<% end %>
