<%= render "admin/shared/bread_crumb", name: {title: "MassTime", bread_crumb: "Top"} %>
<!-- Main content -->
<section class="content">
  <div class="container-fluid">
    <div class="row">
      <div class="col-12">
        <div class="card">
          <div class="card-header">
            <h3 class="card-title">Manage mass time</h3>
          </div>
          <!-- /.card-header -->
          <div class="card-body">
            <table id="example2" class="table table-bordered table-hover">
              <thead>
                <tr>
                  <th>Name</th>
                  <th>Content</th>
                  <th>Created at</th>
                  <th>Updated at</th>
                  <th>Action</th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td><%= @mass_time&.name || "-" %></td>
                  <td><%= @mass_time&.content&.html_safe || "-"  %></td>
                  <td><%= @mass_time&.created_at || "-"  %></td>
                  <td><%= @mass_time&.updated_at || "-"  %></td>
                  <td>
                    <% unless @mass_time.blank? %>
                      <%= link_to "Edit", edit_admin_mass_time_path(@mass_time), class: "btn btn-success" %>
                      <%= link_to "Delete", admin_mass_time_path(@mass_time), method: :delete, class: "btn btn-danger", data: { confirm: "Are you sure?" } %>
                    <% end %>
                  </td>
                </tr>
              </tbody>
            </table>
          </div>
          <!-- /.card-body -->
        </div>
      </div>
      <!-- /.col -->
    </div>
    <!-- /.row -->
  </div>
  <!-- /.container-fluid -->
</section>
<section class="section-btn-group">
  <div class="section-btn-group-wrap">
    <%= link_to "Create", new_admin_mass_time_path, class: "btn btn-info" %>
  </div>
</section>
