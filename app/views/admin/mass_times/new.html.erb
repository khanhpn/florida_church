<%= render "admin/shared/bread_crumb", name: {title: "Create mass time", bread_crumb: "New"} %>
<%= form_with model: @mass_time, url: admin_mass_times_path do |form| %>
  <section class="content">
    <div class="container-fluid">
      <%= render "form", form: form %>
      <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
    <div class="section-btn-group-wrap">
      <%= link_to "Back", admin_mass_times_path, class: "btn btn-info" %>
      <%= form.submit "Create", class: "btn btn-success", id: "id_submit_mass_time" %>
    </div>
  </section>
<% end %>
