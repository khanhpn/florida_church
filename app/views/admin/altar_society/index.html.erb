<%= render "admin/shared/bread_crumb", name: {title: "Altar Society", bread_crumb: "Top"} %>
<!-- Main content -->
<section class="content">
  <div class="container-fluid">
    <div class="row">
      <div class="col-12">
        <div class="card">
          <div class="card-header">
            <h3 class="card-title">Manage altar society</h3>
          </div>
          <!-- /.card-header -->
          <div class="card-body">
            <table id="example2" class="table table-bordered table-hover">
              <thead>
                <tr>
                  <th>Name</th>
                  <th>Display</th>
                  <th>Main article</th>
                  <th>Created at</th>
                  <th>Action</th>
                </tr>
              </thead>
              <tbody>
                <% @altar_societies.each do |altar_society| %>
                  <tr>
                    <td><%= link_to altar_society&.name, altar_society&.name_link %></td>
                    <td><%= altar_society&.display ? "Yes" : "No"  %></td>
                    <td><%= altar_society&.is_main_article ? "Yes" : "No"  %></td>
                    <td><%= altar_society&.created_at || "-"  %></td>
                    <td>
                      <% unless altar_society.blank? %>
                        <%= link_to "Show", admin_altar_society_path(altar_society), class: "btn btn-primary" %>
                        <%= link_to "Edit", edit_admin_altar_society_path(altar_society), class: "btn btn-success" %>
                        <%= link_to "Delete", admin_altar_society_path(altar_society), method: :delete, class: "btn btn-danger", data: { confirm: "Are you sure?" } %>
                      <% end %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
          <!-- /.card-body -->
        </div>
      </div>
      <!-- /.col -->
    </div>
    <!-- /.row -->
  </div>
  <!-- /.container-fluid -->
</section>
<section class="section-btn-group">
  <div class="section-btn-group-wrap">
    <%= link_to "Create", new_admin_altar_society_path, class: "btn btn-info" %>
  </div>
</section>
