<%= render "admin/shared/bread_crumb", name: {title: "Create bulletin", bread_crumb: "New"} %>
<%= form_with model: @bulletin, url: admin_bulletin_index_path do |form| %>
  <section class="content">
    <div class="container-fluid">
      <%= render "form", form: form %>
      <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
    <div class="section-btn-group-wrap">
      <%= link_to "Back", admin_bulletin_index_path, class: "btn btn-info" %>
      <%= form.submit "Create", class: "btn btn-success", id: "id_submit_bulletin" %>
    </div>
  </section>
<% end %>
