<%= render "admin/shared/bread_crumb", name: {title: "Edit bulletin", bread_crumb: "Edit"} %>
<%= form_with model: @bulletin, url: admin_bulletin_path(@bulletin), method: :patch do |form| %>
  <section class="content">
    <div class="container-fluid">
      <%= render "form", form: form %>
      <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
    <div class="section-btn-group-wrap">
      <%= link_to "Back", admin_bulletin_index_path, class: "btn btn-info" %>
      <%= form.submit "Update", class: "btn btn-success", id: "id_submit_bulletin" %>
    </div>
  </section>
<% end %>
