<%= render "admin/shared/bread_crumb", name: {title: "Create history church", bread_crumb: "New"} %>
<%= form_with model: @history_church, url: admin_history_church_index_path do |form| %>
  <section class="content">
    <div class="container-fluid">
      <%= render "form", form: form %>
      <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
    <div class="section-btn-group-wrap">
      <%= link_to "Back", admin_history_church_index_path, class: "btn btn-info" %>
      <%= form.submit "Create", class: "btn btn-success", id: "id_submit_history_church" %>
    </div>
  </section>
<% end %>
