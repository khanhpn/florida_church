<%= render "admin/shared/bread_crumb", name: {title: "Edit deacon", bread_crumb: "Edit"} %>
<%= form_with model: @deacon, url: admin_deacon_path(@deacon), method: :patch do |form| %>
  <section class="content">
    <div class="container-fluid">
      <%= render "admin/shared_form/form", form: form, item: @deacon, title_name: "Update deacon" %>
      <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
    <div class="section-btn-group-wrap">
      <%= link_to "Back", admin_deacon_index_path, class: "btn btn-info" %>
      <%= form.submit "Update", class: "btn btn-success", id: "id_submit" %>
    </div>
  </section>
<% end %>
