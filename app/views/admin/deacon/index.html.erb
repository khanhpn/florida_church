<%= render "admin/shared/bread_crumb", name: {title: "Deacon", bread_crumb: "Top"} %>
<!-- Main content -->
<section class="content">
  <div class="container-fluid">
    <div class="row">
      <div class="col-12">
        <div class="card">
          <div class="card-header">
            <h3 class="card-title">Manage deacon</h3>
          </div>
          <!-- /.card-header -->
          <div class="card-body">
            <table id="example2" class="table table-bordered table-hover">
              <thead>
                <tr>
                  <th>Name</th>
                  <th>Content</th>
                  <th>Display</th>
                  <th>Display Order</th>
                  <th>Created at</th>
                  <th>Updated at</th>
                  <th>Action</th>
                </tr>
              </thead>
              <tbody>
                <% @deacons.each do |deacon| %>
                  <tr>
                    <td><%= deacon&.name || "-" %></td>
                    <td><%= deacon&.content&.html_safe || "-"  %></td>
                    <td><%= deacon&.display || "-"  %></td>
                    <td><%= deacon&.display_order || "-"  %></td>
                    <td><%= deacon&.created_at || "-"  %></td>
                    <td><%= deacon&.updated_at || "-"  %></td>
                    <td>
                      <% unless deacon.blank? %>
                        <%= link_to "Edit", edit_admin_deacon_path(deacon), class: "btn btn-success" %>
                        <%= link_to "Delete", admin_deacon_path(deacon), method: :delete, class: "btn btn-danger" %>
                      <% end %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
          <!-- /.card-body -->
        </div>
      </div>
      <!-- /.col -->
    </div>
    <!-- /.row -->
  </div>
  <!-- /.container-fluid -->
</section>
<section class="section-btn-group">
  <div class="section-btn-group-wrap">
    <%= link_to "Create", new_admin_deacon_path, class: "btn btn-info" %>
  </div>
</section>
