<%= render "admin/shared/bread_crumb", name: {title: "Edit art craft group", bread_crumb: "Edit"} %>
<%= form_with model: @art_craft_group, url: admin_art_craft_group_path(@art_craft_group), method: :patch do |form| %>
  <section class="content">
    <div class="container-fluid">
      <%= render "admin/shared_form/form", form: form, item: @art_craft_group, title_name: "Update art craft group" %>
      <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
    <div class="section-btn-group-wrap">
      <%= link_to "Back", admin_art_craft_group_index_path, class: "btn btn-info" %>
      <%= form.submit "Update", class: "btn btn-success", id: "id_submit" %>
    </div>
  </section>
<% end %>
