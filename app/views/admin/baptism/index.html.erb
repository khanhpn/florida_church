<%= render "admin/shared/bread_crumb", name: {title: "Baptism", bread_crumb: "Top"} %>
<!-- Main content -->
<section class="content">
  <div class="container-fluid">
    <div class="row">
      <div class="col-12">
        <div class="card">
          <div class="card-header">
            <h3 class="card-title">Manage baptism</h3>
          </div>
          <!-- /.card-header -->
          <div class="card-body">
            <table id="example2" class="table table-bordered table-hover">
              <thead>
                <tr>
                  <th>Name</th>
                  <th>Content</th>
                  <th>Display</th>
                  <th>Display Order</th>
                  <th>Created at</th>
                  <th>Updated at</th>
                  <th>Action</th>
                </tr>
              </thead>
              <tbody>
                <% @baptisms.each do |baptism| %>
                  <tr>
                    <td><%= baptism&.name || "-" %></td>
                    <td><%= baptism&.content&.html_safe || "-"  %></td>
                    <td><%= baptism&.display || "-"  %></td>
                    <td><%= baptism&.display_order || "-"  %></td>
                    <td><%= baptism&.created_at || "-"  %></td>
                    <td><%= baptism&.updated_at || "-"  %></td>
                    <td>
                      <% unless baptism.blank? %>
                        <%= link_to "Edit", edit_admin_baptism_path(baptism), class: "btn btn-success" %>
                        <%= link_to "Delete", admin_baptism_path(baptism), method: :delete, class: "btn btn-danger" %>
                      <% end %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
          <!-- /.card-body -->
        </div>
      </div>
      <!-- /.col -->
    </div>
    <!-- /.row -->
  </div>
  <!-- /.container-fluid -->
</section>
<section class="section-btn-group">
  <div class="section-btn-group-wrap">
    <%= link_to "Create", new_admin_baptism_path, class: "btn btn-info" %>
  </div>
</section>
