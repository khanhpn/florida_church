<%= render "admin/shared/bread_crumb", name: {title: "Create category gallery", bread_crumb: "New"} %>
<%= form_with model: @category_gallery, url: admin_category_gallery_index_path do |form| %>
  <section class="content">
    <div class="container-fluid">
      <%= render "form", form: form, item: @category_gallery, title_name: "Create category gallery" %>
      <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
    <div class="section-btn-group-wrap">
      <%= link_to "Back", admin_category_gallery_index_path, class: "btn btn-info" %>
      <%= form.submit "Create", class: "btn btn-success", id: "id_submit" %>
    </div>
  </section>
<% end %>
