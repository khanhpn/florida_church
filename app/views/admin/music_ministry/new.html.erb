<%= render "admin/shared/bread_crumb", name: {title: "Create music ministry", bread_crumb: "New"} %>
<%= form_with model: @music_ministry, url: admin_music_ministry_index_path do |form| %>
  <section class="content">
    <div class="container-fluid">
      <%= render "form", form: form %>
      <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
    <div class="section-btn-group-wrap">
      <%= link_to "Back", admin_music_ministry_index_path, class: "btn btn-info" %>
      <%= form.submit "Create", class: "btn btn-success", id: "id_submit_music_ministry" %>
    </div>
  </section>
<% end %>
