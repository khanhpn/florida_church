<%= render "admin/shared/bread_crumb", name: {title: "Music ministries", bread_crumb: "Top"} %>
<!-- Main content -->
<section class="content">
  <div class="container-fluid">
    <div class="row">
      <div class="col-12">
        <div class="card">
          <div class="card-header">
            <h3 class="card-title">Manage music ministries</h3>
          </div>
          <!-- /.card-header -->
          <div class="card-body">
            <table id="example2" class="table table-bordered table-hover">
              <thead>
                <tr>
                  <th>Name</th>
                  <th>Content</th>
                  <th>Display</th>
                  <th>Display Order</th>
                  <th>Created at</th>
                  <th>Updated at</th>
                  <th>Action</th>
                </tr>
              </thead>
              <tbody>
                <% @music_ministries.each do |music_ministry| %>
                  <tr>
                    <td><%= music_ministry&.name || "-" %></td>
                    <td><%= music_ministry&.content&.html_safe || "-"  %></td>
                    <td><%= music_ministry&.display || "-"  %></td>
                    <td><%= music_ministry&.display_order || "-"  %></td>
                    <td><%= music_ministry&.created_at || "-"  %></td>
                    <td><%= music_ministry&.updated_at || "-"  %></td>
                    <td>
                      <% unless music_ministry.blank? %>
                        <%= link_to "Edit", edit_admin_music_ministry_path(music_ministry), class: "btn btn-success" %>
                        <%= link_to "Delete", admin_music_ministry_path(music_ministry), method: :delete, class: "btn btn-danger" %>
                      <% end %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
          <!-- /.card-body -->
        </div>
      </div>
      <!-- /.col -->
    </div>
    <!-- /.row -->
  </div>
  <!-- /.container-fluid -->
</section>

<section class="section-btn-group">
  <div class="section-btn-group-wrap">
    <%= link_to "Create", new_admin_music_ministry_path, class: "btn btn-info" %>
  </div>
</section>
