<%= render "admin/shared/bread_crumb", name: {title: "Create respect life", bread_crumb: "New"} %>
<%= form_with model: @respect_life, url: admin_respect_life_index_path do |form| %>
  <section class="content">
    <div class="container-fluid">
      <%= render "admin/shared_form/form", form: form, item: @respect_life, title_name: "Create respect life" %>
      <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
    <div class="section-btn-group-wrap">
      <%= link_to "Back", admin_respect_life_index_path, class: "btn btn-info" %>
      <%= form.submit "Create", class: "btn btn-success", id: "id_submit" %>
    </div>
  </section>
<% end %>
