<%= render "admin/shared/bread_crumb", name: {title: "Reconciliation", bread_crumb: "Top"} %>
<!-- Main content -->
<section class="content">
  <div class="container-fluid">
    <div class="row">
      <div class="col-12">
        <div class="card">
          <div class="card-header">
            <h3 class="card-title">Manage reconciliation</h3>
          </div>
          <!-- /.card-header -->
          <div class="card-body">
            <table id="example2" class="table table-bordered table-hover">
              <thead>
                <tr>
                  <th>Name</th>
                  <th>Display</th>
                  <th>Main article</th>
                  <th>Created at</th>
                  <th>Action</th>
                </tr>
              </thead>
              <tbody>
                <% @reconciliations.each do |reconciliation| %>
                  <tr>
                    <td><%= reconciliation&.name || "-" %></td>
                    <td><%= reconciliation&.display ? "Yes" : "No"  %></td>
                    <td><%= reconciliation&.is_main_article ? "Yes" : "No"  %></td>
                    <td><%= reconciliation&.created_at || "-"  %></td>
                    <td>
                      <% unless reconciliation.blank? %>
                        <%= link_to "Show", admin_reconciliation_path(reconciliation), class: "btn btn-primary" %>
                        <%= link_to "Edit", edit_admin_reconciliation_path(reconciliation), class: "btn btn-success" %>
                        <%= link_to "Delete", admin_reconciliation_path(reconciliation), method: :delete, class: "btn btn-danger" %>
                      <% end %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
          <!-- /.card-body -->
        </div>
      </div>
      <!-- /.col -->
    </div>
    <!-- /.row -->
  </div>
  <!-- /.container-fluid -->
</section>
<section class="section-btn-group">
  <div class="section-btn-group-wrap">
    <%= link_to "Create", new_admin_reconciliation_path, class: "btn btn-info" %>
  </div>
</section>
