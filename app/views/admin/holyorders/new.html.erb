<%= render "admin/shared/bread_crumb", name: {title: "Create holyorder", bread_crumb: "New"} %>
<%= form_with model: @holyorder, url: admin_holyorder_index_path do |form| %>
  <section class="content">
    <div class="container-fluid">
      <%= render "form", form: form %>
      <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
    <div class="section-btn-group-wrap">
      <%= link_to "Back", admin_holyorder_index_path, class: "btn btn-info" %>
      <%= form.submit "Create", class: "btn btn-success", id: "id_submit_holyorder" %>
    </div>
  </section>
<% end %>
