<%= render "admin/shared/bread_crumb", name: {title: "Create linked logo", bread_crumb: "New"} %>
<%= form_with model: @linked_logo, url: admin_logo_link_index_path do |form| %>
  <section class="content">
    <div class="container-fluid">
      <%= render "form", form: form %>
      <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
    <div class="section-btn-group-wrap">
      <%= link_to "Back", admin_logo_link_index_path, class: "btn btn-info" %>
      <%= form.submit "Create", class: "btn btn-success" %>
    </div>
  </section>
<% end %>
