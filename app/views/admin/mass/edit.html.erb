<%= render "admin/shared/bread_crumb", name: {title: "Edit mass", bread_crumb: "Edit"} %>
<%= form_with model: @mass, url: admin_mass_path(@mass), method: :patch do |form| %>
  <section class="content">
    <div class="container-fluid">
      <%= render "form", form: form %>
      <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
    <div class="section-btn-group-wrap">
      <%= link_to "Back", admin_mass_index_path, class: "btn btn-info" %>
      <%= form.submit "Update", class: "btn btn-success", id: "id_submit_mass" %>
    </div>
  </section>
<% end %>
