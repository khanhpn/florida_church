<%= render "admin/shared/bread_crumb", name: {title: "Teaching mass", bread_crumb: "Top"} %>
<!-- Main content -->
<section class="content">
  <div class="container-fluid">
    <div class="row">
      <div class="col-12">
        <div class="card">
          <div class="card-header">
            <h3 class="card-title">Manage teaching mass</h3>
          </div>
          <!-- /.card-header -->
          <div class="card-body">
            <table id="example2" class="table table-bordered table-hover">
              <thead>
                <tr>
                  <th>Name</th>
                  <th>Display</th>
                  <th>Main article</th>
                  <th>Created at</th>
                  <th>Action</th>
                </tr>
              </thead>
              <tbody>
                <% @teaching_masses.each do |teaching_mass| %>
                  <tr>
                    <td><%= link_to teaching_mass&.name, teaching_mass&.name_link %></td>
                    <td><%= teaching_mass&.display ? "Yes" : "No"  %></td>
                    <td><%= teaching_mass&.is_main_article ? "Yes" : "No"  %></td>
                    <td><%= teaching_mass&.created_at || "-"  %></td>
                    <td>
                      <% unless teaching_mass.blank? %>
                        <%= link_to "Show", admin_teaching_mass_path(teaching_mass), class: "btn btn-primary" %>
                        <%= link_to "Edit", edit_admin_teaching_mass_path(teaching_mass), class: "btn btn-success" %>
                        <%= link_to "Delete", admin_teaching_mass_path(teaching_mass), method: :delete, class: "btn btn-danger", data: { confirm: "Are you sure?" } %>
                      <% end %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
          <!-- /.card-body -->
        </div>
      </div>
      <!-- /.col -->
    </div>
    <!-- /.row -->
  </div>
  <!-- /.container-fluid -->
</section>
<section class="section-btn-group">
  <div class="section-btn-group-wrap">
    <%= link_to "Create", new_admin_teaching_mass_path, class: "btn btn-info" %>
  </div>
</section>
