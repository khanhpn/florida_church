<%= render "admin/shared/bread_crumb", name: {title: "Engraved Brick", bread_crumb: "Top"} %>
<!-- Main content -->
<section class="content">
  <div class="container-fluid">
    <div class="row">
      <div class="col-12">
        <div class="card">
          <div class="card-header">
            <h3 class="card-title">Manage engraved brick</h3>
          </div>
          <!-- /.card-header -->
          <div class="card-body">
            <table id="example2" class="table table-bordered table-hover">
              <thead>
                <tr>
                  <th>Name</th>
                  <th>Display</th>
                  <th>Main article</th>
                  <th>Created at</th>
                  <th>Action</th>
                </tr>
              </thead>
              <tbody>
                <% @pray_gardens.each do |pray_garden| %>
                  <tr>
                    <td><%= link_to pray_garden&.name, pray_garden&.name_link %></td>
                    <td><%= pray_garden&.display ? "Yes" : "No"  %></td>
                    <td><%= pray_garden&.is_main_article ? "Yes" : "No"  %></td>
                    <td><%= pray_garden&.created_at || "-"  %></td>
                    <td>
                      <% unless pray_garden.blank? %>
                        <%= link_to "Show", admin_pray_garden_path(pray_garden), class: "btn btn-primary" %>
                        <%= link_to "Edit", edit_admin_pray_garden_path(pray_garden), class: "btn btn-success" %>
                        <%= link_to "Delete", admin_pray_garden_path(pray_garden), method: :delete, class: "btn btn-danger" %>
                      <% end %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
          <!-- /.card-body -->
        </div>
      </div>
      <!-- /.col -->
    </div>
    <!-- /.row -->
  </div>
  <!-- /.container-fluid -->
</section>
<section class="section-btn-group">
  <div class="section-btn-group-wrap">
    <%= link_to "Create", new_admin_pray_garden_path, class: "btn btn-info" %>
  </div>
</section>
