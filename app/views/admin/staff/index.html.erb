<%= render "admin/shared/bread_crumb", name: {title: "Staff", bread_crumb: "Top"} %>
<!-- Main content -->
<section class="content">
  <div class="container-fluid">
    <div class="row">
      <div class="col-12">
        <div class="card">
          <div class="card-header">
            <h3 class="card-title">Manage staff</h3>
          </div>
          <!-- /.card-header -->
          <div class="card-body">
            <table id="example2" class="table table-bordered table-hover">
              <thead>
                <tr>
                  <th>Name</th>
                  <th>Content</th>
                  <th>Display</th>
                  <th>Display Order</th>
                  <th>Created at</th>
                  <th>Updated at</th>
                  <th>Action</th>
                </tr>
              </thead>
              <tbody>
                <% @staffs.each do |staff| %>
                  <tr>
                    <td><%= staff&.name || "-" %></td>
                    <td><%= staff&.content&.html_safe || "-"  %></td>
                    <td><%= staff&.display || "-"  %></td>
                    <td><%= staff&.display_order || "-"  %></td>
                    <td><%= staff&.created_at || "-"  %></td>
                    <td><%= staff&.updated_at || "-"  %></td>
                    <td>
                      <% unless staff.blank? %>
                        <%= link_to "Edit", edit_admin_staff_path(staff), class: "btn btn-success" %>
                        <%= link_to "Delete", admin_staff_path(staff), method: :delete, class: "btn btn-danger" %>
                      <% end %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
          <!-- /.card-body -->
        </div>
      </div>
      <!-- /.col -->
    </div>
    <!-- /.row -->
  </div>
  <!-- /.container-fluid -->
</section>
<section class="section-btn-group">
  <div class="section-btn-group-wrap">
    <%= link_to "Create", new_admin_staff_path, class: "btn btn-info" %>
  </div>
</section>
